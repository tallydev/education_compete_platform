<% _mark_items = @recruit.activity.mark_items %>

<% if @browser == :phone %>
  <%= render partial: "phone_mark", locals: {_mark_items: _mark_items} %>  
<% else %>
  <%= render partial: "pc_mark", locals: {_mark_items: _mark_items}  %>
<% end %>