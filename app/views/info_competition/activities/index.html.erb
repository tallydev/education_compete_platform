<p id="notice"><%= notice %></p>

<h1>Listing Info Competition Activities</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @info_competition_activities.each do |info_competition_activity| %>
      <tr>
        <td><%= link_to 'Show', info_competition_activity %></td>
        <td><%= link_to 'Edit', edit_info_competition_activity_path(info_competition_activity) %></td>
        <td><%= link_to 'Destroy', info_competition_activity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activity', new_info_competition_activity_path %>
