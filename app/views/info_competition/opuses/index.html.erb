<p id="notice"><%= notice %></p>

<h1>Listing Info Competition Opuses</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @info_competition_opuses.each do |info_competition_opus| %>
      <tr>
        <td><%= link_to 'Show', info_competition_opus %></td>
        <td><%= link_to 'Edit', edit_info_competition_opus_path(info_competition_opus) %></td>
        <td><%= link_to 'Destroy', info_competition_opus, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Opus', new_info_competition_opus_path %>
