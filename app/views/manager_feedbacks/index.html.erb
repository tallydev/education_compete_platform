<p id="notice"><%= notice %></p>

<h1>Listing Manager Feedbacks</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @manager_feedbacks.each do |manager_feedback| %>
      <tr>
        <td><%= link_to 'Show', manager_feedback %></td>
        <td><%= link_to 'Edit', edit_manager_feedback_path(manager_feedback) %></td>
        <td><%= link_to 'Destroy', manager_feedback, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Manager feedback', new_manager_feedback_path %>
